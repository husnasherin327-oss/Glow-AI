fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.14.0

# ── LangChain / LangGraph ─────────────────────────────────────────────────────
# langchain-community 0.4.x requires langchain-core v1.x (NOT 0.3.x)
# All three packages below must be in the same v1 generation
langchain-community==0.4.1
langchain-huggingface>=1.0.0        # BUG FIX: was missing — HuggingFaceEndpoint lives here now
langgraph==1.1.6

# ── Hugging Face ──────────────────────────────────────────────────────────────
# langchain-huggingface requires huggingface-hub>=0.33.4
# The old pin of ==1.11.0 was wrong (1.11 doesn't exist; latest is ~0.26.x range)
huggingface-hub>=0.33.0

# ── Search ────────────────────────────────────────────────────────────────────
duckduckgo-search==6.3.7

